info:
  name: Get Random Quote
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/{{versionApi}}/quotes/random?size=1"
  params:
    - name: size
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
