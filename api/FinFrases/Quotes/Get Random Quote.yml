info:
  name: Get Random Quote
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/{{versionApi}}/quotes/random"
  params:
    - name: size
      value: "1"
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
