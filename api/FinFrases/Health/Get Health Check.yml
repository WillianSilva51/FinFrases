info:
  name: Get Health Check
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/{{versionApi}}/health"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
