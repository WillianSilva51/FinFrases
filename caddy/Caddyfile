localhost {
	@backend {
		path /api/*
	}

	handle @backend {
		reverse_proxy finfrases-api:8000
	}

	handle {
		root * /usr/share/caddy/finfrases

		rewrite / /html/index.html
	
		try_files /html{path} /html{path}.html {path} {path}/ /html/index.html
	
		file_server
	}
}
